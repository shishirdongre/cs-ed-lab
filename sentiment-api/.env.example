# Copy to .env (gitignored). Used when running server locally (npm run dev).

PORT=3000

# Google Sheets (same as terraform - for /save-user and /reflect)
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=
