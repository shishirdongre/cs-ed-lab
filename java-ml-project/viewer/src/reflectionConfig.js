/**
 * Set to true to allow submitting empty reflections (bypasses min length and confidence).
 * Useful for testing. Set to false for production.
 */
export const ALLOW_EMPTY_REFLECTION_FOR_TESTING = true
