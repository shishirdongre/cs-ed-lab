# Google Form Questions for Java Sentiment Analysis Lab

**Project Title:** Understanding Conceptual Transfer from Imperative Rule-Based Programming to AI/ML Data-Driven Computation Approaches

**Instructions for Form Creator:** Each question section corresponds to a specific stage of the Java program. Code links should point to the specific line numbers in the GitHub repository. Format: `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L[START]-L[END]`

---

## PRE-SURVEY SECTION

### Background Questions

**Question 1:** What programming languages have you used before? (Check all that apply)
- [ ] Java
- [ ] Python
- [ ] C/C++
- [ ] JavaScript
- [ ] Other: _______________

**Question 2:** How many programming courses have you completed?
- [ ] 1 course
- [ ] 2 courses
- [ ] 3 courses
- [ ] 4 or more courses

**Question 3:** What was the most recent programming course you completed?
- [ ] CS 46A (Introduction to Programming) or equivalent
- [ ] CS 46B (Data Structures) or equivalent
- [ ] CS 146 (Data Structures and Algorithms) or equivalent
- [ ] Other: _______________

**Question 4:** How long ago did you complete your first programming course?
- [ ] Less than 6 months ago
- [ ] 6-12 months ago
- [ ] 1-2 years ago
- [ ] More than 2 years ago

**Question 5:** Have you worked with machine learning or artificial intelligence before?
- [ ] Yes, extensively (multiple projects or courses)
- [ ] Yes, a little bit (one project or brief exposure)
- [ ] No, never

**Question 6:** If you have experience with machine learning, what was it? (Check all that apply)
- [ ] Used pre-built ML tools (like TensorFlow, scikit-learn)
- [ ] Took a machine learning course
- [ ] Worked on an ML project
- [ ] Read about ML concepts
- [ ] Used AI tools (like ChatGPT, image generators)
- [ ] No experience
- [ ] Other: _______________

**Question 7:** On a scale from 1 (not confident) to 5 (very confident), how confident do you feel about reading and understanding Java code?
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Question 8:** On a scale from 1 (not confident) to 5 (very confident), how confident do you feel about writing Java code from scratch?
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Question 9:** Which Java concepts are you most comfortable with? (Check all that apply)
- [ ] Variables and data types
- [ ] Loops (for, while)
- [ ] Conditionals (if-else)
- [ ] Methods/functions
- [ ] Classes and objects
- [ ] Arrays and collections
- [ ] File I/O
- [ ] Exception handling
- [ ] None of the above

**Question 10:** Which Java concepts do you find most challenging? (Check all that apply)
- [ ] Variables and data types
- [ ] Loops (for, while)
- [ ] Conditionals (if-else)
- [ ] Methods/functions
- [ ] Classes and objects
- [ ] Arrays and collections
- [ ] File I/O
- [ ] Exception handling
- [ ] None of the above

**Question 11:** How do you typically approach learning new programming concepts?
- [ ] I read code examples first
- [ ] I try to write code myself first
- [ ] I watch videos or tutorials
- [ ] I ask questions and discuss with others
- [ ] I combine multiple approaches
- [ ] Other: _______________

**Question 12:** When you see code you don't understand, what do you usually do first?
- [ ] Try to trace through it step by step
- [ ] Look up documentation or explanations
- [ ] Ask someone for help
- [ ] Try modifying it to see what happens
- [ ] Skip it and come back later
- [ ] Other: _______________

**Question 13:** Have you worked with data files (like CSV files) in your programming courses?
- [ ] Yes, frequently
- [ ] Yes, occasionally
- [ ] Yes, but only once or twice
- [ ] No, never

**Question 14:** Have you worked with text processing in your programming courses (like splitting strings, searching text, etc.)?
- [ ] Yes, frequently
- [ ] Yes, occasionally
- [ ] Yes, but only once or twice
- [ ] No, never

**Question 15:** What do you think "machine learning" means? (Write 2-3 sentences in your own words)
[Open text response - minimum 2 sentences]

**Question 16:** What do you think "artificial intelligence" means? (Write 2-3 sentences in your own words)
[Open text response - minimum 2 sentences]

**Question 17:** Can you think of any examples of machine learning or AI that you've encountered in everyday life?
[Open text response]

**Question 18:** How do you think a computer program might learn to classify text as positive or negative? (Write 2-3 sentences)
[Open text response - minimum 2 sentences]

**Question 19:** In traditional programming, you write explicit rules (like "if the word is 'great', then it's positive"). How do you think machine learning might be different?
[Open text response - minimum 2 sentences]

**Question 20:** When you think about machine learning, which Java concepts do you think might be similar or related? (Check all that apply)
- [ ] Variables and data types
- [ ] Loops (for, while)
- [ ] Conditionals (if-else)
- [ ] Methods/functions
- [ ] Classes and objects
- [ ] Arrays and collections
- [ ] File I/O
- [ ] Exception handling
- [ ] I'm not sure yet
- [ ] Other: _______________

**Question 21:** How do you think your experience with Java programming might help you understand machine learning? (Write 2-3 sentences)
[Open text response - minimum 2 sentences]

**Question 22:** How do you think your experience with Java programming might make it harder to understand machine learning? (Write 2-3 sentences)
[Open text response - minimum 2 sentences]

**Question 23:** In Java, you typically write code that follows a specific sequence of steps. How do you think machine learning might be similar or different in terms of how the program works?
[Open text response - minimum 2 sentences]

**Question 24:** On a scale from 1 (not interested) to 5 (very interested), how interested are you in learning about machine learning?
- [ ] 1 - Not interested
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very interested

**Question 25:** What are your expectations for this workshop? What do you hope to learn?
[Open text response - minimum 2 sentences]

**Question 26:** What concerns or worries do you have about learning machine learning concepts?
[Open text response]

**Question 27:** Have you used GitHub or similar code hosting platforms before?
- [ ] Yes, frequently
- [ ] Yes, occasionally
- [ ] Yes, but only once or twice
- [ ] No, never

**Question 28:** Have you used cloud-based development environments (like GitHub Codespaces, Google Colab, Replit) before?
- [ ] Yes, frequently
- [ ] Yes, occasionally
- [ ] Yes, but only once or twice
- [ ] No, never

**Question 29:** What is your major or intended major?
- [ ] Computer Science
- [ ] Software Engineering
- [ ] Computer Engineering
- [ ] Other: _______________

**Question 30:** What year are you in your program?
- [ ] First year
- [ ] Second year
- [ ] Third year
- [ ] Fourth year or higher
- [ ] Graduate student

**Question 31:** On a scale from 1 (not confident) to 5 (very confident), how confident do you feel about your overall programming abilities?
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Question 32:** When learning programming, do you prefer:
- [ ] Visual/graphical tools (like drag-and-drop interfaces)
- [ ] Writing code directly
- [ ] A combination of both
- [ ] No preference

**Question 33:** How do you typically debug code when something doesn't work?
- [ ] Add print statements to see what's happening
- [ ] Use a debugger
- [ ] Read error messages carefully
- [ ] Ask for help
- [ ] Search online for solutions
- [ ] Other: _______________

**Question 34:** What motivates you to learn programming? (Check all that apply)
- [ ] It's required for my major
- [ ] I find it interesting
- [ ] I want to build things
- [ ] Career opportunities
- [ ] Problem-solving is satisfying
- [ ] Other: _______________

---

## STAGE 0: PROGRAM INITIALIZATION

### Question 0.1: Understanding Imports
**Code Section:** Program Imports (Lines 1-30)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L1-L30`

**Predict:**
Look at the import statements at the top of the Java file:
```java
import org.apache.spark.sql.*;
import org.apache.spark.sql.types.*;
import org.apache.spark.ml.*;
import org.apache.spark.ml.feature.*;
import org.apache.spark.ml.classification.*;
// ... and more ...
```

**What do you think these import statements are doing?**
[Open text response - minimum 2 sentences]

**What do you notice about the package names (org.apache.spark...)?**
[Open text response]

**Which imports look familiar or unfamiliar to you?**
[Open text response]

**Instruction:**
Import statements tell Java where to find the classes and libraries that the program will use. These imports bring in functionality from the Apache Spark framework.

**Reveal:**
These import statements load classes from the Apache Spark library. The package names like `org.apache.spark.sql` indicate these are from the Spark SQL module (for working with structured data), `org.apache.spark.ml` is for machine learning, and `org.apache.spark.ml.feature` contains tools for processing features (like text). These imports allow the program to use Spark's data processing and machine learning capabilities.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How are these imports similar to or different from imports you've seen in other Java programs?**
[Open text response]

**CONCEPTUAL TRANSFER:** When you see these Spark imports, what comes to mind from your previous Java experience? How are you making sense of these imports?
[Open text response - minimum 2 sentences]

---

### Question 0.2: Program Initialization
**Code Section:** Program Initialization (Lines 38-60)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L38-L60`

**Predict:**
Look at this code snippet from the beginning of the main method:
```java
SparkConf conf = new SparkConf()
    .setAppName("YelpSentimentAnalysis")
    .setMaster("local[*]")
    // ... more configuration settings ...

SparkSession spark = SparkSession.builder()
    .config(conf)
    .getOrCreate();
```

**What do you think this code is doing? (Don't worry about understanding all the details - just focus on the overall purpose)**
[Open text response - minimum 2 sentences]

**What do you think might be getting set up or initialized here?**
[Open text response]

**Instruction:**
This code sets up the environment and tools needed to run the program. The specific details aren't important right now - just focus on understanding the general purpose.

**Reveal:**
This code initializes Apache Spark, which is a framework for processing data. It's setting up the environment so the program can work with data. Think of it like starting up a tool or engine that the rest of the program will use. The specific configuration settings control how Spark runs, but the main idea is that this prepares Spark to process our data.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this initialization similar to or different from setup code you've seen in other programs?**
[Open text response]

**CONCEPTUAL TRANSFER:** What do you notice about how this code creates and configures objects? Does anything feel familiar or different from what you've seen before?
[Open text response - minimum 2 sentences]

---

### Question 0.3: Dataset Object Import
**Code Section:** Dataset Import (Line 1-30, specifically Dataset)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L1-L30`

**Predict:**
Look at this import statement:
```java
import org.apache.spark.sql.Dataset;
import org.apache.spark.sql.Row;
```

**What do you think `Dataset` and `Row` might be?**
[Open text response - minimum 2 sentences]

**What do you think these are used for in the context of data processing?**
[Open text response]

**Instruction:**
These are types that represent structured data in Spark. Think about how you might organize data in a program.

**Reveal:**
`Dataset<Row>` is Spark's way of representing a table of data - like a spreadsheet with rows and columns. `Row` represents a single row in that table. When you read data from a file, it gets loaded into a Dataset. This is similar to how you might use a 2D array or a list of objects in regular Java, but Dataset is optimized for working with large amounts of data.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is a Dataset similar to or different from data structures you've used before (like arrays or lists)?**
[Open text response]

**CONCEPTUAL TRANSFER:** When you see `Dataset<Row>`, what does it remind you of? How are you thinking about what this might be?
[Open text response - minimum 2 sentences]

---

### Question 0.4: Dataset Object Creation
**Code Section:** Dataset Creation - Reading Data (Lines 149-153)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L149-L153`

**Predict:**
Look at this code snippet:
```java
Dataset<Row> data = spark.read()
    .option("header", "true")
    .option("inferSchema", "false")
    .schema(schema)
    .csv(CSV_FILE);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think `Dataset<Row> data` represents?**
[Open text response]

**What do you think happens when this code runs?**
[Open text response]

**Instruction:**
This code creates a Dataset object by reading data from a file. The Dataset will contain all the data from the CSV file.

**Reveal:**
This code reads a CSV file and creates a `Dataset<Row>` object called `data`. The `.read()` method starts the reading process, `.option("header", "true")` tells Spark that the first row contains column names, `.schema(schema)` specifies the structure of the data, and `.csv(CSV_FILE)` reads from the CSV file. The result is a Dataset object that contains all the data from the file, organized in rows and columns.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is creating a Dataset from a file similar to or different from reading files in other Java programs you've written?**
[Open text response]

**CONCEPTUAL TRANSFER:** When you see `spark.read().csv()`, what does this make you think about? How are you understanding what this code does?
[Open text response - minimum 2 sentences]

---

## STAGE 1: DATA LOADING & PREPARATION

### Question 1.1: Schema Definition
**Code Section:** Data Loading - Schema Definition (Lines 143-146)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L143-L146`

**Predict:**
Look at this code snippet:
```java
StructType schema = new StructType(new StructField[]{
    DataTypes.createStructField("text", DataTypes.StringType, false),
    DataTypes.createStructField("sentiment", DataTypes.StringType, false)
});
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Which parts look familiar or new to you?**
[Open text response]

**Instruction:**
Before we can work with data in a program, we need to tell the computer what structure to expect. This code defines the "shape" of the data file.

**Reveal:**
This code creates a schema (a blueprint) that describes the structure of the CSV file. It tells Java that the file has two columns: "text" (which contains strings) and "sentiment" (which also contains strings). The `false` parameter means these columns cannot be empty. This is similar to defining the columns in a spreadsheet before you start entering data.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What do you notice about how Java describes data types compared to how you might describe them in everyday language?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does defining a schema remind you of? How are you making sense of this way of describing data?
[Open text response - minimum 2 sentences]

---

### Question 1.2: Reading CSV File
**Code Section:** Data Loading - CSV Reading (Lines 149-153)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L149-L153`

**Predict:**
Look at this code snippet:
```java
Dataset<Row> data = spark.read()
    .option("header", "true")
    .option("inferSchema", "false")
    .schema(schema)
    .csv(CSV_FILE);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think the `.option()` calls are for?**
[Open text response]

**Instruction:**
This code reads data from a file. Think about what happens when you open a spreadsheet file - the program needs to know how to interpret the file format.

**Reveal:**
This code reads the CSV file into a Dataset (a structured collection of data). The `.option("header", "true")` tells Spark that the first row contains column names. The `.option("inferSchema", "false")` means "don't guess the data types - use the schema we defined instead." The `.schema(schema)` applies the schema we created earlier, and `.csv(CSV_FILE)` specifies which file to read.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this different from or similar to how you might read a file in other programming you've done?**
[Open text response]

**CONCEPTUAL TRANSFER:** What do you notice about how these methods are called one after another? How does this way of writing code feel to you?
[Open text response - minimum 2 sentences]

---

### Question 1.2b: Understanding Features in Raw Dataset
**Code Section:** Data Loading - Raw Dataset Structure (Lines 143-153)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L143-L153`

**Predict:**
Look at the schema definition and the data that was just loaded:
```java
StructType schema = new StructType(new StructField[]{
    DataTypes.createStructField("text", DataTypes.StringType, false),
    DataTypes.createStructField("sentiment", DataTypes.StringType, false)
});
```

The CSV file contains two columns: "text" (the review text) and "sentiment" (positive or negative).

**What do you think "features" means in the context of this raw dataset?**
[Open text response - minimum 2 sentences]

**What are the features in this dataset before any processing?**
[Open text response]

**What do you think makes something a "feature" in machine learning?**
[Open text response]

**Instruction:**
In machine learning, "features" are the input data that the algorithm uses to make predictions. In the raw dataset, features are the columns or attributes that describe each example.

**Reveal:**
In this raw dataset, the "features" are the columns: "text" (the review text) and "sentiment" (the label). However, "text" is the actual feature we'll use for prediction, while "sentiment" is the label (the answer we're trying to predict). In machine learning, features are the input variables that the model uses to learn patterns and make predictions. Right now, our feature is just raw text - we'll need to process it into a format the computer can work with.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what features are in the raw dataset?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How are features in this dataset similar to or different from features you might use in other programming contexts (like variables or parameters)?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does the term "feature" remind you of? How are you thinking about what makes something a feature in this context?
[Open text response - minimum 2 sentences]

---

### Question 1.3: Label Conversion - withColumn Method
**Code Section:** Data Loading - Label Conversion with withColumn (Lines 156-159)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L156-L159`

**Predict:**
Look at this code snippet:
```java
data = data.withColumn("label", 
    when(col("sentiment").equalTo("positive"), 1)
    .otherwise(0)
).select("text", "label");
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think `.withColumn()` does?**
[Open text response]

**Why do you think the code converts "positive" to 1 and everything else to 0?**
[Open text response]

**Instruction:**
Sometimes we need to change the format of our data to make it easier for the computer to work with. This code is transforming text labels into numbers.

**Reveal:**
This code creates a new column called "label" that converts the text sentiment values into numbers. If the sentiment is "positive", it becomes 1. Otherwise (if it's "negative"), it becomes 0. Then it selects only the "text" and "label" columns, keeping just what we need. Machine learning algorithms often work better with numbers than text labels.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What do you think might happen if we tried to use the text "positive" and "negative" directly instead of converting to numbers?**
[Open text response]

**CONCEPTUAL TRANSFER:** What do you think about converting text like "positive" to a number like 1? How are you making sense of why this conversion happens?
[Open text response - minimum 2 sentences]

---

### Question 1.4: Conditional Logic in Data Transformation
**Code Section:** Data Loading - Conditional Logic (Lines 157-158)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L157-L158`

**Predict:**
Look at this code snippet:
```java
when(col("sentiment").equalTo("positive"), 1)
    .otherwise(0)
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**How is this similar to or different from an if-else statement you might write in regular Java?**
[Open text response]

**Instruction:**
This code uses conditional logic to make decisions based on data values. Think about how you might check a condition and assign a value.

**Reveal:**
This is a conditional expression that works like an if-else statement. It checks if the "sentiment" column equals "positive", and if so, returns 1. Otherwise, it returns 0. This is similar to `if (sentiment.equals("positive")) { return 1; } else { return 0; }` but written in a more compact, functional style that works with data transformations.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What advantages or disadvantages do you see in this style of conditional logic compared to traditional if-else statements?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does this `when().otherwise()` code remind you of? How are you understanding what it does?
[Open text response - minimum 2 sentences]

---

## STAGE 2: DATA SPLITTING

### Question 2.1: Train/Test Split - randomSplit Method
**Code Section:** Data Splitting - randomSplit (Lines 79-81)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L79-L81`

**Predict:**
Look at this code snippet:
```java
Dataset<Row>[] splits = data.randomSplit(new double[]{1.0 - TEST_SIZE, TEST_SIZE}, RANDOM_SEED);
Dataset<Row> trainData = splits[0];
Dataset<Row> testData = splits[1];
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think `TEST_SIZE` might be, and why would we split the data?**
[Open text response]

**What do you think `RANDOM_SEED` is for?**
[Open text response]

**Instruction:**
When learning, it's important to have some examples to study and some examples to test yourself on. This code divides the data into two groups.

**Reveal:**
This code randomly splits the dataset into two parts. The first part (80% of the data) becomes `trainData` - this is what the computer will use to learn patterns. The second part (20% of the data) becomes `testData` - this is what we'll use later to check if the computer learned correctly. The `RANDOM_SEED` ensures we get the same split every time we run the program, which makes results reproducible.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think we don't use the same data for both learning and testing?**
[Open text response]

**CONCEPTUAL TRANSFER:** What do you think about splitting the data into two parts? How are you making sense of why we do this?
[Open text response - minimum 2 sentences]

---

### Question 2.2: Understanding Array Access After Split
**Code Section:** Data Splitting - Array Access (Lines 80-81)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L80-L81`

**Predict:**
Look at this code snippet:
```java
Dataset<Row> trainData = splits[0];
Dataset<Row> testData = splits[1];
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think the code uses array indices [0] and [1]?**
[Open text response]

**Instruction:**
After splitting, the result is stored in an array. This code extracts the individual parts.

**Reveal:**
The `randomSplit()` method returns an array with two datasets. `splits[0]` is the first dataset (training data, 80%), and `splits[1]` is the second dataset (test data, 20%). This is similar to how you might access elements in a regular Java array.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is accessing data from this array similar to or different from accessing elements in arrays you've used before?**
[Open text response]

---

## STAGE 3: FEATURE ENGINEERING - PIPELINE BUILDING

### Question 3.1: Tokenizer - Setting Input and Output Columns
**Code Section:** Feature Engineering - Tokenizer Configuration (Lines 169-171)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L169-L171`

**Predict:**
Look at this code snippet:
```java
Tokenizer tokenizer = new Tokenizer()
    .setInputCol("text")
    .setOutputCol("words");
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think "tokenize" means in the context of text processing?**
[Open text response]

**What do you think `.setInputCol()` and `.setOutputCol()` are doing?**
[Open text response]

**Instruction:**
Computers need to break down text into smaller pieces to analyze it. This code sets up a tool to do that transformation.

**Reveal:**
A Tokenizer splits text into individual words (tokens). For example, "Great food!" becomes ["Great", "food"]. The `.setInputCol("text")` tells it to read from the "text" column, and `.setOutputCol("words")` tells it to put the result in a new column called "words". This is the first step in converting text into a format the computer can analyze.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What challenges do you think might arise when splitting text into words? (Think about punctuation, spaces, etc.)**
[Open text response]

---

### Question 3.1b: Understanding Method Chaining
**Code Section:** Feature Engineering - Method Chaining Pattern (Lines 169-171)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L169-L171`

**Predict:**
Look at this code snippet:
```java
Tokenizer tokenizer = new Tokenizer()
    .setInputCol("text")
    .setOutputCol("words");
```

**What do you notice about how the methods are called?**
[Open text response]

**How is this different from calling methods separately?**
[Open text response]

**Instruction:**
This code uses a pattern called "method chaining" where multiple methods are called on the same object in sequence.

**Reveal:**
This is called method chaining. Each method (like `.setInputCol()` and `.setOutputCol()`) returns the same object, so you can call another method on the result. This is equivalent to writing: `Tokenizer tokenizer = new Tokenizer(); tokenizer.setInputCol("text"); tokenizer.setOutputCol("words");` but more compact. This pattern is common in Java libraries that build configuration objects.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Have you seen this pattern of chaining method calls before? Where?**
[Open text response]

---

### Question 3.2: StopWordsRemover
**Code Section:** Feature Engineering - StopWordsRemover (Lines 174-176)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L174-L176`

**Predict:**
Look at this code snippet:
```java
StopWordsRemover stopWordsRemover = new StopWordsRemover()
    .setInputCol("words")
    .setOutputCol("filtered_words");
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think "stop words" might be?**
[Open text response]

**Instruction:**
Not all words are equally important when analyzing text. This code removes words that don't carry much meaning.

**Reveal:**
StopWordsRemover removes common words like "the", "a", "and", "is", "in", etc. These words appear in almost every sentence but don't help distinguish between positive and negative reviews. By removing them, we focus on words that actually matter for sentiment analysis. The filtered words go into a new column called "filtered_words".

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Can you think of any situations where removing stop words might not be a good idea?**
[Open text response]

---

### Question 3.3: HashingTF - Converting Words to Numbers
**Code Section:** Feature Engineering - HashingTF Configuration (Lines 179-182)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L179-L182`

**Predict:**
Look at this code snippet:
```java
HashingTF hashingTF = new HashingTF()
    .setInputCol("filtered_words")
    .setOutputCol("rawFeatures")
    .setNumFeatures(10000);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think "features" means in machine learning?**
[Open text response]

**What do you think the number 10000 represents?**
[Open text response]

**Instruction:**
Computers work with numbers, not words. This code converts words into numerical representations that the computer can process.

**Reveal:**
HashingTF (Hashing Term Frequency) converts words into numbers. It counts how many times each word appears and represents this as a vector (a list of numbers). The `.setNumFeatures(10000)` creates space for up to 10,000 different features (word positions). This is like creating a giant spreadsheet where each column represents a possible word, and the numbers show how many times that word appeared in the text.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this different from how you might represent text in a regular programming assignment?**
[Open text response]

---

### Question 3.3b: Understanding Hashing Concept
**Code Section:** Feature Engineering - HashingTF Concept (Lines 179-182)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L179-L182`

**Predict:**
Look at this code snippet:
```java
HashingTF hashingTF = new HashingTF()
    .setInputCol("filtered_words")
    .setOutputCol("rawFeatures")
    .setNumFeatures(10000);
```

**The name includes "Hashing" - what do you think "hashing" means in programming?**
[Open text response]

**Why do you think we use hashing here instead of just creating a list of all words?**
[Open text response]

**Instruction:**
Hashing is a technique that converts data into a fixed-size value. Think about how this might help when dealing with many different words.

**Reveal:**
Hashing converts each word into a number between 0 and 9999 using a hash function. This is more efficient than keeping a list of all possible words because: (1) we don't need to store the vocabulary, (2) we can handle new words we haven't seen before, and (3) it's faster. The trade-off is that different words might hash to the same number (called a collision), but this is usually acceptable for machine learning.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What advantages or disadvantages do you see in using hashing versus keeping a list of all words?**
[Open text response]

---

### Question 3.4: IDF
**Code Section:** Feature Engineering - IDF (Lines 185-187)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L185-L187`

**Predict:**
Look at this code snippet:
```java
IDF idf = new IDF()
    .setInputCol("rawFeatures")
    .setOutputCol("features");
```

**IDF stands for "Inverse Document Frequency". What do you think this might mean?**
[Open text response - minimum 2 sentences]

**What do you think "Inverse" and "Document Frequency" might refer to in the context of text processing?**
[Open text response]

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Instruction:**
Not all words are equally important. Some words appear in many documents, while others are rare. This code adjusts the importance of words based on how common or rare they are.

**Reveal:**
IDF (Inverse Document Frequency) adjusts the weight (importance) of words based on how frequently they appear across all documents. "Document Frequency" refers to how many documents contain a particular word. "Inverse" means we do the opposite - words that appear in many reviews (like "food" or "service") get lower weights because they're not very distinctive. Words that appear in only a few reviews get higher weights because they're more unique and informative. This helps the model focus on words that actually help distinguish positive from negative reviews.

**Why do you think IDF might be needed in this machine learning pipeline?**
[Open text response - minimum 2 sentences]

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think rare words might be more important than common words for this task?**
[Open text response]

---

### Question 3.5: NaiveBayes Classifier - Configuration
**Code Section:** Feature Engineering - NaiveBayes Configuration (Lines 190-194)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L190-L194`

**Predict:**
Look at this code snippet:
```java
NaiveBayes naiveBayes = new NaiveBayes()
    .setFeaturesCol("features")
    .setLabelCol("label")
    .setModelType("multinomial")
    .setSmoothing(1.0);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think a "classifier" does?**
[Open text response]

**What do you think "smoothing" might mean?**
[Open text response]

**Instruction:**
This code sets up the learning algorithm - the part that will figure out patterns in the data.

**Reveal:**
This creates a Naive Bayes classifier, which is a machine learning algorithm that learns patterns from examples. It uses the "features" (the processed word counts) to predict the "label" (positive or negative). The "multinomial" model type is designed for counting data (like word frequencies). The "smoothing" parameter (1.0) helps handle words that the algorithm hasn't seen before - it prevents the algorithm from being completely certain about something it's never encountered.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this different from writing an if-else statement to classify reviews?**
[Open text response]

---

### Question 3.5b: Understanding ModelType Parameter
**Code Section:** Feature Engineering - NaiveBayes ModelType (Line 193)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L193`

**Predict:**
Look at this code snippet:
```java
.setModelType("multinomial")
```

**What do you think "multinomial" means?**
[Open text response]

**Why do you think the code specifies a model type?**
[Open text response]

**Instruction:**
Different types of data require different mathematical approaches. This parameter tells the algorithm which approach to use.

**Reveal:**
"Multinomial" refers to a type of probability distribution that's good for counting data - like how many times each word appears. There are other model types (like "bernoulli" or "gaussian") that work better for different kinds of data. By specifying "multinomial", we're telling the algorithm that our features are word counts, not just whether words are present or absent.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think it matters what type of data distribution we use?**
[Open text response]

---

### Question 3.5c: Understanding Smoothing Parameter
**Code Section:** Feature Engineering - NaiveBayes Smoothing (Line 194)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L194`

**Predict:**
Look at this code snippet:
```java
.setSmoothing(1.0);
```

**What do you think "smoothing" means in this context?**
[Open text response]

**What problem do you think smoothing might solve?**
[Open text response]

**Instruction:**
When working with data, sometimes we encounter things we haven't seen before. Smoothing helps handle these cases gracefully.

**Reveal:**
Smoothing (also called Laplace smoothing) adds a small amount to every count to prevent zero probabilities. For example, if a word never appeared in positive reviews during training, without smoothing, the algorithm would think "if I see this word, it's impossible for the review to be positive." Smoothing says "even if we haven't seen it, it's still possible, just unlikely." This makes the model more robust to new words it hasn't encountered.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What might happen if we set smoothing to 0.0 instead of 1.0?**
[Open text response]

---

### Question 3.6: Pipeline Assembly
**Code Section:** Feature Engineering - Pipeline Assembly (Lines 197-204)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L197-L204`

**Predict:**
Look at this code snippet:
```java
Pipeline pipeline = new Pipeline()
    .setStages(new PipelineStage[]{
        tokenizer,
        stopWordsRemover,
        hashingTF,
        idf,
        naiveBayes
    });
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think these components are put together in this order?**
[Open text response]

**Instruction:**
This code connects all the processing steps we've set up into a single workflow.

**Reveal:**
This creates a Pipeline that chains together all the processing steps in order. Data flows through: Tokenizer ? StopWordsRemover ? HashingTF ? IDF ? NaiveBayes. Each step takes the output from the previous step and transforms it. This is like an assembly line where raw text goes in one end and predictions come out the other end. The order matters - you can't remove stop words before you've split the text into words!

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this pipeline approach similar to or different from how you might organize code in a regular Java program?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does putting these steps together in a Pipeline remind you of? How are you thinking about how data flows through these steps?
[Open text response - minimum 2 sentences]

---

### Question 3.6b: Understanding Feature Extraction Process
**Code Section:** Feature Engineering - Feature Extraction Stages (Lines 177-192)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L177-L192`

**Predict:**
Look at all the feature extraction stages we've seen:
```java
Tokenizer tokenizer = new Tokenizer()
    .setInputCol("text")
    .setOutputCol("words");

StopWordsRemover stopWordsRemover = new StopWordsRemover()
    .setInputCol("words")
    .setOutputCol("filtered_words");

HashingTF hashingTF = new HashingTF()
    .setInputCol("filtered_words")
    .setOutputCol("rawFeatures")
    .setNumFeatures(10000);

IDF idf = new IDF()
    .setInputCol("rawFeatures")
    .setOutputCol("features");
```

**What do you think the feature extraction code is doing overall?**
[Open text response - minimum 3 sentences]

**How do you think the features change from the raw dataset to after feature extraction?**
[Open text response - minimum 2 sentences]

**Why do you think we need to transform the raw text into numerical features?**
[Open text response]

**Instruction:**
Feature extraction is the process of converting raw data (like text) into a numerical format that machine learning algorithms can process. This code transforms text through multiple stages.

**Reveal:**
The feature extraction code is transforming raw text into numerical features that the classifier can use. It starts with raw text, splits it into words (Tokenizer), removes common words (StopWordsRemover), converts words to numbers (HashingTF), and then adjusts the importance of words (IDF). The final output is a numerical vector (a list of numbers) that represents the text in a way the computer can analyze. This is necessary because machine learning algorithms work with numbers, not text - they need numerical features to learn patterns and make predictions.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what the feature extraction code is doing?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this process of transforming text to numbers similar to or different from data transformations you've done in other programming contexts?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does this process of converting text to numerical features remind you of? How are you thinking about why we need to transform the data this way?
[Open text response - minimum 2 sentences]

---

## STAGE 4: TRAINING

### Question 4.1: Model Training - The fit() Method
**Code Section:** Training - fit() Method (Lines 105-107)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L105-L107`

**Predict:**
Look at this code snippet:
```java
long startTime = System.currentTimeMillis();
PipelineModel model = pipeline.fit(trainData);
long trainingTime = System.currentTimeMillis() - startTime;
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think `.fit()` does?**
[Open text response]

**Why do you think the code measures time?**
[Open text response]

**Instruction:**
This is where the actual learning happens. The computer looks at all the training examples and figures out patterns.

**Reveal:**
The `.fit()` method trains the model by having it learn patterns from the training data. It goes through all the training examples, analyzes the relationships between words and sentiments, and builds an internal model that can make predictions. The time measurement helps us understand how long the learning process takes. This is different from writing explicit rules - the computer discovers the patterns on its own.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this process of "fitting" or "training" different from how you might write a program that classifies text using if-else statements?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does "training" or "fitting" a model remind you of? How are you thinking about what happens when `.fit()` is called?
[Open text response - minimum 2 sentences]

---

### Question 4.1b: Time Measurement Pattern
**Code Section:** Training - Time Measurement (Lines 104-107)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L104-L107`

**Predict:**
Look at this code snippet:
```java
long startTime = System.currentTimeMillis();
PipelineModel model = pipeline.fit(trainData);
long trainingTime = System.currentTimeMillis() - startTime;
```

**What do you think `System.currentTimeMillis()` does?**
[Open text response]

**Why do you think the code records the time before and after calling `.fit()`?**
[Open text response]

**Instruction:**
This code measures how long an operation takes. Think about why you might want to know how long something takes.

**Reveal:**
`System.currentTimeMillis()` returns the current time in milliseconds since January 1, 1970. By recording the time before and after the `.fit()` call, we can calculate how long the training took. This is useful for understanding performance and comparing different approaches. The difference between `startTime` and the time after gives us `trainingTime`.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**When might you want to measure how long code takes to run?**
[Open text response]

---

## STAGE 5: MAKING PREDICTIONS

### Question 5.1: Prediction on Test Data - transform() Method
**Code Section:** Making Predictions - transform() (Lines 114-116)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L114-L116`

**Predict:**
Look at this code snippet:
```java
long predStartTime = System.currentTimeMillis();
Dataset<Row> predictions = model.transform(testData);
long predTime = System.currentTimeMillis() - predStartTime;
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think `.transform()` does?**
[Open text response]

**What's the difference between `.fit()` and `.transform()`?**
[Open text response]

**Instruction:**
Now that the model has learned, we test it on data it hasn't seen before to see how well it performs.

**Reveal:**
The `.transform()` method uses the trained model to make predictions on new data. It takes the test data (which the model hasn't seen during training), processes it through the pipeline, and produces predictions. This is like taking a test after studying - you're applying what you learned to new problems. The time measurement shows how quickly the model can make predictions.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think we test on data the model hasn't seen before?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does making predictions on new data remind you of? How are you thinking about the difference between training and making predictions?
[Open text response - minimum 2 sentences]

---

### Question 5.1b: Understanding fit() vs transform()
**Code Section:** Training vs Prediction - fit() vs transform() (Lines 105 vs 114)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L105` and `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L114`

**Predict:**
Look at these two code snippets:

Training:
```java
PipelineModel model = pipeline.fit(trainData);
```

Prediction:
```java
Dataset<Row> predictions = model.transform(testData);
```

**What's the difference between `.fit()` and `.transform()`?**
[Open text response - minimum 2 sentences]

**Why do you think we call `.fit()` on the pipeline but `.transform()` on the model?**
[Open text response]

**Instruction:**
These two methods serve different purposes in machine learning. One learns from data, the other applies what was learned.

**Reveal:**
`.fit()` is called on the pipeline to train/learn from the training data. It creates a trained model. `.transform()` is called on the trained model to apply it to new data and make predictions. You can think of `.fit()` as "studying" and `.transform()` as "taking a test". Once a model is trained with `.fit()`, you use `.transform()` to make predictions on new data.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What would happen if you tried to call `.fit()` twice on the same data? What about calling `.transform()` before `.fit()`?**
[Open text response]

---

## STAGE 6: EVALUATION

### Question 6.1: Accuracy Calculation - Evaluator Setup
**Code Section:** Evaluation - Evaluator Configuration (Lines 218-220)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L218-L220`

**Predict:**
Look at this code snippet:
```java
MulticlassClassificationEvaluator evaluator = new MulticlassClassificationEvaluator()
    .setLabelCol("label")
    .setPredictionCol("prediction");
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think "evaluator" means in this context?**
[Open text response]

**Why do you think the code specifies "label" and "prediction" columns?**
[Open text response]

**Instruction:**
This code sets up a tool that will compare predictions to actual answers.

**Reveal:**
This creates an evaluator object that knows which columns contain the actual answers ("label") and which contain the model's predictions ("prediction"). The evaluator will use these columns to calculate various metrics that tell us how well the model performed.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think we need a separate evaluator object instead of just calculating accuracy directly?**
[Open text response]

---

### Question 6.1b: Accuracy Calculation - evaluate() Method
**Code Section:** Evaluation - Calculating Accuracy (Line 222)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L222`

**Predict:**
Look at this code snippet:
```java
double accuracy = evaluator.setMetricName("accuracy").evaluate(predictions);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think "accuracy" means in this context?**
[Open text response]

**What do you think `.evaluate()` does?**
[Open text response]

**Instruction:**
After making predictions, we need to check how good they are. This code calculates a measure of correctness.

**Reveal:**
This code uses the evaluator to calculate accuracy. `.setMetricName("accuracy")` tells it which metric to calculate, and `.evaluate(predictions)` does the actual calculation by comparing predictions to actual labels. Accuracy is the percentage of predictions that were correct. For example, if the model correctly predicted 8,000 out of 10,000 reviews, the accuracy would be 80%.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What other ways might you measure if a program is working correctly?**
[Open text response]

**CONCEPTUAL TRANSFER:** What does checking if predictions are correct remind you of? How are you thinking about how to know if a program is working?
[Open text response - minimum 2 sentences]

---

### Question 6.1c: Multiple Metrics Calculation
**Code Section:** Evaluation - Multiple Metrics (Lines 222-225)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L222-L225`

**Predict:**
Look at this code snippet:
```java
double accuracy = evaluator.setMetricName("accuracy").evaluate(predictions);
double precision = evaluator.setMetricName("weightedPrecision").evaluate(predictions);
double recall = evaluator.setMetricName("weightedRecall").evaluate(predictions);
double f1 = evaluator.setMetricName("f1").evaluate(predictions);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think the code calculates multiple different metrics (accuracy, precision, recall, f1)?**
[Open text response]

**What do you think "precision" and "recall" might measure?**
[Open text response]

**Instruction:**
Different metrics tell us different things about how the model performs. This code calculates several metrics to get a complete picture.

**Reveal:**
This code calculates four different metrics: accuracy (overall correctness), precision (how reliable positive predictions are), recall (how many positive cases were found), and F1 (a combined measure). Each metric tells us something different. For example, a model might have high accuracy but poor recall (missing many positive reviews), so we need multiple metrics to understand the full picture.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why might accuracy alone not be enough to understand how well a model works?**
[Open text response]

---

### Question 6.2: Confusion Matrix - Array Initialization
**Code Section:** Evaluation - Confusion Matrix Array (Lines 286-287)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L286-L287`

**Predict:**
Look at this code snippet:
```java
long[][] matrix = new long[2][2];
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think it's a 2x2 array?**
[Open text response]

**What do you think will be stored in this array?**
[Open text response]

**Instruction:**
This code creates a two-dimensional array to store counts of different prediction outcomes.

**Reveal:**
This creates a 2x2 two-dimensional array (an array of arrays). It has 2 rows and 2 columns, creating 4 cells total. This will store counts of: [0][0] = actual negative, predicted negative; [0][1] = actual negative, predicted positive; [1][0] = actual positive, predicted negative; [1][1] = actual positive, predicted positive. The 2x2 size matches our two classes: negative (0) and positive (1).

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is a 2D array different from a regular 1D array you might have used before?**
[Open text response]

---

### Question 6.2b: Confusion Matrix - Collecting Results
**Code Section:** Evaluation - Collecting Predictions (Line 289)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L289`

**Predict:**
Look at this code snippet:
```java
List<Row> results = predictions.select("label", "prediction").collectAsList();
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think `.select()` does?**
[Open text response]

**What do you think `.collectAsList()` does?**
[Open text response]

**Instruction:**
This code extracts specific columns from the predictions and brings them into memory as a regular Java list.

**Reveal:**
This code selects only the "label" (actual answer) and "prediction" (model's guess) columns from the predictions dataset, then collects all the rows into a regular Java List. The `.select()` chooses which columns to keep, and `.collectAsList()` brings the data from the distributed dataset into local memory so we can loop through it with a regular for loop.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think we need to collect the data into a list instead of working with it directly?**
[Open text response]

---

### Question 6.2c: Confusion Matrix - Counting in Loop
**Code Section:** Evaluation - Counting Predictions (Lines 290-309)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L290-L309`

**Predict:**
Look at this code snippet:
```java
for (Row row : results) {
    int actual, predicted;
    // ... code to get actual and predicted values ...
    matrix[actual][predicted]++;
}
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What does `matrix[actual][predicted]++` do?**
[Open text response]

**Why do you think we use the actual and predicted values as array indices?**
[Open text response]

**Instruction:**
This code loops through each prediction result and counts how many times each combination of actual and predicted values occurs.

**Reveal:**
This loop goes through each prediction result. For each row, it extracts the actual label and predicted label, then increments the count in the matrix at position [actual][predicted]. For example, if actual=1 (positive) and predicted=1 (positive), it increments matrix[1][1], which counts correct positive predictions. The `++` operator adds 1 to the current count.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this use of a 2D array as a counting structure similar to or different from other uses of arrays you've seen?**
[Open text response]

---

### Question 6.2d: Type Handling in Confusion Matrix
**Code Section:** Evaluation - Type Handling (Lines 294-306)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L294-L306`

**Predict:**
Look at this code snippet:
```java
// Get actual label
if (row.get(0) instanceof Integer) {
    actual = row.getInt(0);
} else {
    actual = ((Double) row.get(0)).intValue();
}

// Get predicted label
if (row.get(1) instanceof Integer) {
    predicted = row.getInt(1);
} else {
    predicted = ((Double) row.get(1)).intValue();
}
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think the code checks `instanceof Integer`?**
[Open text response]

**What do you think `.intValue()` does?**
[Open text response]

**Instruction:**
Sometimes data can come in different types. This code handles both possibilities to make the code more robust.

**Reveal:**
This code checks what type the data is (Integer or Double) and extracts the integer value accordingly. Spark can return numbers as either Integer or Double depending on the context. The `instanceof` check determines the type, then we either use `.getInt()` directly for Integers or convert a Double to int using `.intValue()`. This makes the code work regardless of which type Spark returns.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think it's important to handle different data types like this?**
[Open text response]

---

### Question 6.2e: Calculating Sensitivity and Specificity
**Code Section:** Evaluation - Sensitivity/Specificity Calculation (Lines 269-270)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L269-L270`

**Predict:**
Look at this code snippet:
```java
double sensitivity = (double) truePositives / (truePositives + falseNegatives);
double specificity = (double) trueNegatives / (trueNegatives + falsePositives);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think "sensitivity" and "specificity" might measure?**
[Open text response]

**Why do you think the code casts to `(double)`?**
[Open text response]

**Instruction:**
These are additional metrics that give us more detailed information about model performance.

**Reveal:**
Sensitivity (also called recall) measures how well the model finds positive cases - it's the proportion of actual positives that were correctly identified. Specificity measures how well the model finds negative cases - it's the proportion of actual negatives that were correctly identified. The `(double)` cast ensures we get decimal division instead of integer division (which would truncate to 0 or 1).

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why might sensitivity and specificity be useful in addition to accuracy?**
[Open text response]

---

## STAGE 7: TESTING WITH SAMPLE REVIEWS

### Question 7.1: Testing Individual Reviews - Creating DataFrame
**Code Section:** Testing - Creating DataFrame for Single Review (Lines 351-357)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L351-L357`

**Predict:**
Look at this code snippet:
```java
Dataset<Row> reviewData = spark.createDataFrame(
    Arrays.asList(RowFactory.create(review, 0)), // dummy label
    new StructType(new StructField[]{
        DataTypes.createStructField("text", DataTypes.StringType, false),
        DataTypes.createStructField("label", DataTypes.IntegerType, false)
    })
);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think there's a "dummy label" of 0?**
[Open text response]

**What do you think `RowFactory.create()` does?**
[Open text response]

**Instruction:**
This code creates a dataset from a single piece of text. Think about how you might convert a single value into the format the model expects.

**Reveal:**
This code creates a small dataset with just one review. `RowFactory.create(review, 0)` creates a row with the review text and a dummy label of 0. `Arrays.asList()` puts this single row into a list, and `spark.createDataFrame()` converts it into a Dataset with the specified schema. The dummy label is needed because the schema requires a label column, but we don't know the actual label yet - that's what we're trying to predict!

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Why do you think we need to create a DataFrame instead of just passing the text string directly to the model?**
[Open text response]

---

### Question 7.1b: Testing Individual Reviews - Making Prediction
**Code Section:** Testing - Making Prediction on Single Review (Line 360)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L360`

**Predict:**
Look at this code snippet:
```java
Dataset<Row> prediction = model.transform(reviewData);
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What do you think will be in the `prediction` dataset?**
[Open text response]

**Instruction:**
This code applies the trained model to make a prediction on the new review.

**Reveal:**
This uses the trained model to make a prediction on the single review. The `prediction` dataset will contain the original review text, the dummy label, and most importantly, a new "prediction" column with the model's guess (0 for negative, 1 for positive). This is the same `.transform()` method we used on the test set, but now applied to a single new review.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is using a trained model different from writing a program with explicit rules to classify text?**
[Open text response]

---

### Question 7.1c: Testing Individual Reviews - Extracting Result
**Code Section:** Testing - Extracting Prediction Result (Lines 361-369)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L361-L369`

**Predict:**
Look at this code snippet:
```java
Row result = prediction.select("prediction").collectAsList().get(0);

// Handle both Integer and Double types from Spark
int predictedLabel;
if (result.get(0) instanceof Integer) {
    predictedLabel = result.getInt(0);
} else {
    predictedLabel = ((Double) result.get(0)).intValue();
}
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think the code uses `.get(0)`?**
[Open text response]

**Why do you think there's type checking again (instanceof)?**
[Open text response]

**Instruction:**
After making a prediction, we need to extract the actual prediction value from the dataset.

**Reveal:**
This code extracts the prediction value from the dataset. `.select("prediction")` gets just the prediction column, `.collectAsList()` brings it into memory as a list, and `.get(0)` gets the first (and only) row since we only predicted on one review. Then it handles the type (Integer or Double) to get the integer prediction value (0 or 1).

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**What do you notice about the pattern of extracting data from Spark datasets?**
[Open text response]

---

### Question 7.1d: Testing Individual Reviews - Loop Through Samples
**Code Section:** Testing - Loop Through Sample Reviews (Lines 346-379)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L346-L379`

**Predict:**
Look at this code snippet:
```java
for (int i = 0; i < sampleReviews.length; i++) {
    String review = sampleReviews[i][0];
    String expected = sampleReviews[i][1];
    // ... code to make prediction and compare ...
}
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**Why do you think the code loops through multiple sample reviews?**
[Open text response]

**What do you think `sampleReviews[i][0]` and `sampleReviews[i][1]` represent?**
[Open text response]

**Instruction:**
This code tests the model on multiple example reviews to demonstrate how it works.

**Reveal:**
This loop goes through each review in the `sampleReviews` array. For each review, it extracts the review text (`sampleReviews[i][0]`) and the expected sentiment (`sampleReviews[i][1]`), makes a prediction, and compares the prediction to the expected result. This demonstrates the model working on real examples and shows how many it gets right.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**How is this loop similar to or different from loops you've written in other Java programs?**
[Open text response]

---

### Question 7.1e: Testing Individual Reviews - Comparing Results
**Code Section:** Testing - Comparing Prediction to Expected (Lines 371-373)
**Code Link:** `https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L371-L373`

**Predict:**
Look at this code snippet:
```java
String predicted = (predictedLabel == 1) ? "positive" : "negative";
String status = expected.equals(predicted) ? "✅" : "❌";
if (expected.equals(predicted)) correct++;
```

**What do you think this code is doing?**
[Open text response - minimum 2 sentences]

**What does the `?` operator do?**
[Open text response]

**Why do you think the code counts how many are correct?**
[Open text response]

**Instruction:**
After making a prediction, we compare it to the expected answer to see if the model was right.

**Reveal:**
This code converts the numeric prediction (0 or 1) back to text ("negative" or "positive"), compares it to the expected answer, and marks it with ✅ if correct or ❌ if wrong. It also increments a counter (`correct++`) to keep track of how many predictions were correct. This is a ternary operator (`condition ? valueIfTrue : valueIfFalse`) - a compact way to write an if-else statement.

**Reflect:**
**On a scale from 1 (not confident) to 5 (very confident), how confident do you feel that you understood what this code did?**
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Have you seen the `?` operator before? How is it different from a regular if-else statement?**
[Open text response]

---

## POST-SURVEY SECTION

### Reflection Questions

**Question 1:** What machine learning concepts did you learn from this workshop?
[Open text response - minimum 3 sentences]

**Question 2:** What did you find most difficult or confusing about the code?
[Open text response - minimum 2 sentences]

**Question 3:** As you worked through this code, what connections did you make to your prior Java programming experience?
[Open text response - minimum 3 sentences]

**Question 4:** What Java concepts or patterns did you find yourself thinking about while reading this machine learning code?
[Open text response - minimum 2 sentences]

**Question 5:** When you saw something new in the machine learning code, how did you try to understand it? What did you compare it to?
[Open text response - minimum 2 sentences]

**Question 6:** What felt familiar about this code compared to Java programs you've written? What felt completely different?
[Open text response - minimum 2 sentences]

**Question 7:** On a scale from 1 (not confident) to 5 (very confident), how confident do you feel about understanding machine learning concepts after this workshop?
- [ ] 1 - Not confident
- [ ] 2
- [ ] 3
- [ ] 4
- [ ] 5 - Very confident

**Question 8:** When you encountered something new in the machine learning code, how did your Java knowledge help or hinder your understanding?
[Open text response - minimum 2 sentences]

**Question 9:** What analogies or comparisons did you make between Java programming and machine learning while working through this code?
[Open text response - minimum 2 sentences]

**Question 10:** If you were to explain machine learning to someone who knows Java but not machine learning, what would you tell them?
[Open text response - minimum 3 sentences]

**Question 11:** What would help you better understand machine learning concepts? (e.g., more examples, different explanations, hands-on practice, etc.)
[Open text response]

---

## NOTES FOR FORM CREATOR

### Complete Question List with Code Sections:

**PRE-SURVEY (30 questions):** Background, experience, expectations

**STAGE 0: PROGRAM INITIALIZATION**
- Question 0.1: Understanding Imports (Lines 1-30)
- Question 0.2: Program Initialization (Lines 38-60)
- Question 0.3: Dataset Object Import (Lines 1-30)
- Question 0.4: Dataset Object Creation (Lines 149-153)

**STAGE 1: DATA LOADING & PREPARATION**
- Question 1.1: Schema Definition (Lines 143-146)
- Question 1.2: Reading CSV File (Lines 149-153)
- Question 1.3: Label Conversion - withColumn (Lines 156-159)
- Question 1.4: Conditional Logic (Lines 157-158)

**STAGE 2: DATA SPLITTING**
- Question 2.1: Train/Test Split - randomSplit (Lines 79-81)
- Question 2.2: Array Access After Split (Lines 80-81)

**STAGE 3: FEATURE ENGINEERING - PIPELINE BUILDING**
- Question 3.1: Tokenizer - Input/Output Columns (Lines 169-171)
- Question 3.1b: Method Chaining Pattern (Lines 169-171)
- Question 3.2: StopWordsRemover (Lines 174-176)
- Question 3.3: HashingTF - Converting Words (Lines 179-182)
- Question 3.3b: Understanding Hashing Concept (Lines 179-182)
- Question 3.4: IDF (Lines 185-187)
- Question 3.5: NaiveBayes Configuration (Lines 190-194)
- Question 3.5b: ModelType Parameter (Line 193)
- Question 3.5c: Smoothing Parameter (Line 194)
- Question 3.6: Pipeline Assembly - Array of Stages (Lines 197-204)
- Question 3.6b: PipelineStage Array (Lines 198-204)

**STAGE 4: TRAINING**
- Question 4.1: Model Training - fit() Method (Lines 105-107)
- Question 4.1b: Time Measurement Pattern (Lines 104-107)

**STAGE 5: MAKING PREDICTIONS**
- Question 5.1: Prediction - transform() Method (Lines 114-116)
- Question 5.1b: Understanding fit() vs transform() (Lines 105 vs 114)

**STAGE 6: EVALUATION**
- Question 6.1: Accuracy - Evaluator Setup (Lines 218-220)
- Question 6.1b: Accuracy - evaluate() Method (Line 222)
- Question 6.1c: Multiple Metrics Calculation (Lines 222-225)
- Question 6.2: Confusion Matrix - Array Initialization (Lines 286-287)
- Question 6.2b: Confusion Matrix - Collecting Results (Line 289)
- Question 6.2c: Confusion Matrix - Counting in Loop (Lines 290-309)
- Question 6.2d: Type Handling (Lines 294-306)
- Question 6.2e: Sensitivity/Specificity Calculation (Lines 269-270)

**STAGE 7: TESTING WITH SAMPLE REVIEWS**
- Question 7.1: Creating DataFrame for Single Review (Lines 351-357)
- Question 7.1b: Making Prediction on Single Review (Line 360)
- Question 7.1c: Extracting Result (Lines 361-369)
- Question 7.1d: Loop Through Samples (Lines 346-379)
- Question 7.1e: Comparing Results (Lines 371-373)

**POST-SURVEY (10 questions):** Reflection and learning outcomes

**TOTAL: ~70+ questions covering all stages of the program**

### Form Setup Tips:
1. Use "Section" breaks in Google Forms to separate each stage
2. For code links, use the format provided - they will open directly to the relevant lines
3. Set minimum character requirements for open text responses (as indicated)
4. Use "Multiple choice" for confidence scales
5. Use "Short answer" or "Paragraph" for open text responses
6. Make the "Reveal" sections collapsible or show them after submission if possible

### Question Design Principles Applied:
- ✅ No leading questions - students form their own understanding
- ✅ Open-ended prompts encourage qualitative responses
- ✅ Predict → Instruction → Reveal → Reflect structure
- ✅ Questions ask "what do you think" rather than "what is"
- ✅ Reflection questions encourage metacognition
- ✅ Questions connect to prior programming knowledge
- ✅ Granular questions on specific code sections within functions
- ✅ Questions about programming patterns (method chaining, polymorphism, etc.)
- ✅ Questions about data types and type handling
- ✅ Questions about specific method calls and their purposes

### GitHub Code Links Format:
All code links follow this format:
`https://github.com/shishirdongre/cs-ed-lab/blob/main/java-ml-project/YelpSentimentAnalysisSpark.java#L[START]-L[END]`

Replace [START] and [END] with the actual line numbers from the question descriptions above.
